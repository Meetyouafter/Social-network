

const initialState = {
  friends: [
    { id: 1, name: 'First' },
    { id: 2, name: 'Second' },
    { id: 3, name: 'Third' },
  ],
}

const sidebarReducer = (state = initialState, action) => {
  return state;
};

export default sidebarReducer;
